# DB credentials — never commit these
*.env

# SSH private keys
id_ed25519*
id_rsa*
*.pem
*.key
!*.pub

# Local targets — keep out of version control, use conf/targets.yml.example
conf/targets.yml

# Editor and OS noise
.DS_Store
*.swp
*~
